A web-based dashboard for tracking stock data, revenue, and business transcation of the company with dynamic charts and reports.
---

## ⚙️ Setup Instructions

### 1️. Clone the repository
### 2. Install Dependencies
``bash
npm install
``
### 3. Run the mock API server
``bash
npx json-server --watch db.json --port 3001
``

#### This starts your local mock API at:
``bash
http://localhost:3001/companies
http://localhost:3001/accounts?companyId=c1
http://localhost:3001/accountData?accountId=a1
``

### 4 In the project directory, you can run:

 `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.


